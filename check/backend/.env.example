# Обязательно для продакшена: сгенерируйте свой ключ (openssl rand -hex 32)
SECRET_KEY=your-secret-key-change-in-production

# Домен сайта — через запятую без пробелов (для CORS). Для разработки можно *
ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# OpenSanctions API — trial: 50 req/mo. Ключ: https://www.opensanctions.org/account/
OPENSANCTIONS_API_KEY=

# Вход через Google: OAuth 2.0 в Google Cloud Console, redirect URI: https://ваш-домен/api/auth/google/callback
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
